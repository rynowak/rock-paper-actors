apiVersion: apps/v1
kind: Deployment
metadata:
  name: robbybot
  labels:
    app: robbybot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: robbybot
  template:
    metadata:
      labels:
        app: robbybot
      annotations:
        actions.io/enabled: "true"
        actions.io/id: "robbybot"
        actions.io/port: "80"
        actions.io/config: "zipkin"
    spec:
      containers:
      - name: robbybot
        image: rynowak.azurecr.io/rochambot/robbybot:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
